import { DispatchDashboard } from '@/components/admin/DispatchDashboard'
import { defaultLocale } from '@/lib/i18n'

export default function AdminDispatchPage() {
  return <DispatchDashboard locale={defaultLocale} />
}

